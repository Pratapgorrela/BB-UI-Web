# Context Summary — F13 Cart & Checkout

## Session Summary
- **Date:** 2026-07-13
- **Branch:** `feature/F13-cart-checkout` (created off `main`, up to date with `origin/main`)
- **IMPORTANT:** User explicitly said **DO NOT COMMIT OR PUSH** this session. All work is
  in the **working tree only**. Do not commit/push unless the user asks. (The heartbeat
  auto-save hook tried to push — that was correctly declined.)

### Accomplished (F13 COMPLETE — checklist steps 129–139 all `[x]`, project now 83/161 = 52%)
- Locked a new **Cart & Checkout** contract section in `instructions/contract.md`
  (entities CartItem/Coupon/PaymentSummary/Order; endpoints GET /coupons,
  POST /checkout/summary, POST /orders) + change-log row.
- Built the full `src/features/cart/` vertical slice + `useCartStore` + mock layer + 3 pages.
- Activated every previously-dead "add to cart" button across the app + cart badges.
- Verified: **typecheck ✓, build ✓, Vitest 45/45 ✓, Playwright 29/29 ✓**, 3 screenshots.

### F13 ADDENDUM — details bottom sheet + sticky cart bar (2026-07-13, BUILT & VERIFIED)
User approved building the popup flow from their Figma mockups (scope: **category pages
only**; ✓ tap **removes** — both confirmed via questions). Client-only, contract untouched.
- Category row tap now opens `ServiceDetailSheet` in the reused `Modal` as a bottom sheet
  (hero + overlaid purple DiscountBadge, strikethrough price, "Duration - x", description
  with floating + → ✓ toggle, "Plan includes"/"Recommended" mini-cards w/ Add⟷Added,
  static "Self-Care Sundays" promo banner).
- Sheet is **URL-driven**: `?service=<id>` via new `useServiceSheetParam` (first
  `useSearchParams` in app). Back gesture closes; deep links restore; open pushes
  `state.sheet` → close = navigate(-1) in-app / replace-clear on deep link;
  `preventScrollReset` keeps scroll.
- Sticky cart bar when cart non-empty: page-level fixed (`animate-rise`) + `position="sticky"`
  instance inside the sheet (must be inside the aria-modal dialog for SR reachability).
  Badge = cartItemCount, subtitle "First +N more", Continue → /cart.
- New @theme animation tokens (`--animate-fade-in/slide-up/rise`) — app's first real
  entrance animations (design.md spring spec). **Fixed latent Modal bug:** root `z-modal`
  was a NO-OP class (project z tokens are `--z-*` not `--z-index-*`) → `z-(--z-modal)`.
- Extracted `deriveServiceRelations` (shared w/ ServiceDetailPage), added `isInCart`
  selector, backward-compatible props: Modal `padded`/`ariaLabel`, StickyBottomBar
  `subtitle`/`position`, DiscountBadge `variant`.
- Verified: **typecheck ✓, build ✓, Vitest 49/49 ✓ (45+4 new), Playwright 49/49 ✓**,
  3 screenshots (`<scratchpad>/pw/shots-addendum/`). Video gaps #1, #3 and part of #7
  are now CLOSED; #2 (inline qty stepper on rows), #5, #6 remain open by choice.

### Video gap analysis (2026-07-13, after F13 — user's Downloads/BB.mp4 prototype walkthrough)
Analyzed via ffmpeg frame extraction (ffmpeg on PATH via winget Gyan.FFmpeg). Video covers
Services → Category → Detail → Cart only (NO checkout screens). Screens match our build;
behavioral gaps found, ranked:
1. **Sticky cart summary bar on category + detail pages after add** (badge/price/duration +
   Continue→cart). BIGGEST GAP: our mobile category/detail pages (hideNav) have NO path to
   cart after adding.
2. Category row "+" morphs into inline `− 1 +` qty stepper (ours: toast only).
3. Detail-page "+" toggles to purple ✓ added-state (ours: none).
4. Cart item lists combo contents as "Services" bullets (we have includedServiceIds, unused in cart).
5. Design cart rows have NO qty stepper (qty lives on category rows) — ours has one.
6. Lavender notice strip above sticky bars (dummy-text placeholder slot).
7. Detail "Plan Includes" = labeled thumbnails (ours: checklist) + promo banner we lack.
Prototype numbers are static (badge 2 / INR 450 / "3 Services" don't add up) — structure
compared, not math. Full comparison was delivered to the user in-chat.

### In progress
- None. F13 is done and verified. Nothing partially built.

### Next (user picks per session)
1. **F7 Booking Flow** — needs Availability & Booking + Profile contract locks first (both DRAFT).
2. **F14 Search** — small (5 steps), reuses catalog components; Home search bar already stubs to /services.
3. Optionally **F9 Profile & Addresses** — would replace the interim `checkoutAddresses` list.
4. Optional polish: remaining video gaps — #2 inline `− 1 +` qty stepper on category rows,
   #5 remove cart-row stepper, #6 notice strip above sticky bars.

### Key decisions (confirmed with user)
- **Hybrid cart state:** cart items = client-state in persisted `useCartStore` (`bb-cart`,
  guest-friendly); coupons + payment math = server-authoritative via mock endpoints.
- **Checkout ends at a mock order:** POST /orders (auth-guarded) returns Order w/ ref code
  `BB-YYYYMMDD-XXXX`; scheduling ("Add Slot") deferred to F7 (toast).

### Issues / blockers
- `npm run lint` is broken repo-wide (pre-existing — ESLint flat config missing, blocked on
  package approval per Rule 10). NOT introduced by F13.
- Addresses at checkout are **interim** (`src/features/cart/data/checkoutAddresses.ts`) — F9 hand-off.

## Technical State
### Files created
- `src/features/cart/` — types/cart.ts, types/cart.schema.ts, api/cartApi.ts,
  hooks/{keys,useFetchCoupons,useCheckoutSummary,usePlaceOrder}.ts,
  components/{CartItemCard,PaymentSummaryCard,CouponSection,AddressSelect}.tsx,
  data/checkoutAddresses.ts, index.ts
- `src/store/useCartStore.ts`
- `src/mocks/data/cart.data.ts`, `src/mocks/handlers/cart.mock.ts`, `src/mocks/handlers/cart.mock.test.ts`
- `src/pages/{CartPage,CheckoutPage,OrderConfirmationPage}.tsx`
- ADDENDUM: `src/features/service-catalog/components/ServiceDetailSheet.tsx`,
  `src/features/service-catalog/hooks/useServiceSheetParam.ts`,
  `src/features/service-catalog/utils/serviceRelations.ts` (+ `.test.ts`)

### Files modified
- `instructions/contract.md` (section locked), `instructions/checklist.md` (F13 done, 52%)
- `src/routes/index.tsx` (/cart public; /checkout,/order-confirmation in ProtectedRoute; all hideNav+fullBleed)
- `src/mocks/index.ts` (register cart.mock)
- `src/components/ui/StickyBottomBar.tsx` (new backward-compatible `priceLabel` prop for ₹ output)
- `src/components/layout/TopNav.tsx` (new desktop cart icon + badge)
- `src/features/home/components/{HomeHeader,PopularCombosSection}.tsx`
- `src/pages/{HomePage,ServiceDetailPage,CategoryDetailPage}.tsx` (add-to-cart wired)

### Patterns introduced
- Coupon rejection handled WITHOUT useEffect: `useCheckoutSummary` is a `keepPreviousData`
  query — a bad coupon shows an inline error while the last-good breakdown stays; Place order
  disabled until fixed.
- Shared `priceCart()` in cart.mock.ts (server-authoritative) used by both summary + orders.

### Dependencies
- None added to the project (Rule 10 respected). Playwright installed only in the scratchpad
  harness (not package.json).

### Tests
- Vitest: **49/49 passing** (30 prior + 15 cart mocks + 4 serviceRelations).
- Browser (scratchpad Playwright): **29/29** F13 flow (`pw/f13.mjs`) + **49/49** addendum
  sheet flow (`pw/f13-addendum.mjs`) — run with dev server on port 5199.

## Resume Instructions
1. `cd BB-UI-Web` — you'll be on `feature/F13-cart-checkout` with the F13 work uncommitted.
2. Read `instructions/checklist.md` (F13 completion note at the bottom of the F13 section) and this file.
3. Confirm state: `npm run typecheck && npm run build && npx vitest run` (all should pass).
4. Do NOT commit/push unless the user asks. If they ask to commit, use `F13, Step ## — Description` per Rule 7.
5. To browser-verify again: `npm run dev -- --port 5199`, then the scratchpad harness at
   `<scratchpad>/pw/f13.mjs` (needs `PLAYWRIGHT_BROWSERS_PATH=%LOCALAPPDATA%/ms-playwright`).
6. Ask the user which feature is next (F7 / F14 / F9) — they choose per session.
