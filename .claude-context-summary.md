# Claude Context Summary — F15 Track Van

## Session Summary

**Date:** 2026-07-13 → 2026-07-14 (late evening session)

**Accomplished — F15 Track Van, COMPLETE (steps 145–148, all `[x]`):**
- Added + LOCKED new **"Tracking" contract section** in `instructions/contract.md`: `Van`, `GeoPoint`, `VanTracking` entities + `GET /bookings/:id/tracking` (auth, owned-or-404; **422 for COMPLETED/CANCELLED**; `?scenario=error` → 500) + change-log entry. Checklist contract table updated.
- Deterministic status mapping (in contract): PENDING → `NOT_DISPATCHED` (null eta/location); CONFIRMED → `EN_ROUTE` (eta = minutes-to-slot clamped 5–45) or `ARRIVING` ≤15 min; IN_PROGRESS → `ARRIVED` (eta 0, van at destination).
- Mock: `seedVans` (4 vans) assigned by booking-id hash (stable per booking); `tracking.mock.ts` reads merged `allBookings()`, expands specialist from seeds + destination from real Address (`findAddressRecord` + `addressToLine`); van position = destination + eta-scaled offset.
- Slice extends `src/features/booking/` (same domain as F7/F8): types + Zod, `fetchVanTracking`, `bookingKeys.tracking(id)`, `useFetchVanTracking` (30s poll, stops on terminal 404/422, `[TrackVan]` logs).
- UI: route `/bookings/:id/track` (ProtectedRoute, hideNav); `TrackVanPage` = `TrackingStatusHero` (purple gradient, h2 under PageHeader h1, aria-live) + `TrackingMapPlaceholder` (no map SDK — Rule 10) + destination bar + van card (Call = `tel:` anchor) + stylist card + reminder card. 404/422 → DataState empty (not error/retry). `BookingDetailPage` Track Van stub repointed from toast → navigate.
- Verified: typecheck ✓, build ✓, **142/142 Vitest** (13 new), **30/30 Playwright browser checks** + 2 screenshots visually reviewed.
- Updated `instructions/checklist.md`: 145–148 `[x]`, F15 complete note, TOTAL **139/166 (84%)**.
- Updated Claude auto-memory (`project_beautybus_state.md` + `MEMORY.md`).

**In progress:** nothing — F15 is fully done and committed.

**Next (prioritized):**
1. F16 Help & Support (10 steps, contract DRAFT — needs locking; Figma screens exist)
2. F10 Reviews (9 steps, contract DRAFT — needs locking)
3. F17 Terms & Policies (3 steps, static, quickest)
4. F12 Polish & Hardening (6 steps, best done last)

**Key decisions:**
- Tracking is a **point-in-time snapshot** endpoint (client polls; no streaming in MVP mock).
- Tracking slice lives in `features/booking/` (booking-domain sub-resource), not a new feature folder.
- Map = branded placeholder; contract already carries lat/lng so a real map SDK can drop in later.
- 404/422 render as friendly empty states with context CTAs (mirrors F5's 404 handling).

**Blockers/issues:**
- `npm run lint` still fails repo-wide (pre-existing — ESLint flat config blocked on `typescript-eslint` package approval, Rule 10).
- Figma connector needs re-auth (non-interactive session can't OAuth) — Figma 184:6819 unreadable; F15 built to brand language; expect possible design-revision pass like F4/F5/F9.

## Technical State

**Created:** `src/mocks/data/vans.data.ts`, `src/mocks/handlers/tracking.mock.ts` (+ `.test.ts`), `src/features/booking/hooks/useFetchVanTracking.ts`, `src/features/booking/components/TrackingStatusHero.tsx`, `src/features/booking/components/TrackingMapPlaceholder.tsx`, `src/pages/TrackVanPage.tsx`

**Modified:** `instructions/contract.md`, `instructions/checklist.md`, `src/features/booking/{types/booking.ts, types/booking.schema.ts, api/bookingApi.ts, hooks/keys.ts, index.ts}`, `src/mocks/index.ts`, `src/pages/BookingDetailPage.tsx`, `src/routes/index.tsx`

**Dependencies:** none added (Rule 10 respected).

**Tests:** 142/142 Vitest passing; 30/30 scratchpad Playwright checks passing.

**Git:** branch `feature/F15-track-van`, commit `bac8b12` — **LOCAL ONLY. User explicitly said "do not push the code until I say" — DO NOT PUSH.** (Heartbeat hook may auto-commit; check `git log` before assuming state.)

## Resume Instructions

1. `cd BB-UI-Web` (the outer folder is a DIFFERENT git repo) and run `git log --oneline -5` on `feature/F15-track-van` — confirm `bac8b12` (or later heartbeat auto-saves) is present.
2. Follow `instructions/MASTER_PROMPT.md` (RESUME mode): read the instructions files, report state (139/166, 84%).
3. Ask the user which feature next (F16 / F10 / F17 / F12) — produce an Intel Report and WAIT for approval before code.
4. If the user asks to push: `git push -u origin feature/F15-track-van` (they merge PRs themselves).
5. Playwright harness pattern this session: install `playwright` inside the session scratchpad (`npm init -y && npm i playwright`); start `npm run dev` detached (piping through `head` kills Vite via SIGPIPE); dev server may land on a non-default port (5177 last time); wait on a success-only selector, not `h1` (PageHeader h1 renders before data).
