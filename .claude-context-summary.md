# Beauty Bus — Session Context Summary

## Session Summary
- **Date:** 2026-07-13 (F3 Home / Landing session)
- **Feature:** F3 — Home / Landing (COMPLETE, verified)
- **Branch:** `feature/F03-home` (branched off `main` @ 87cfd84)
- **User instruction (IMPORTANT):** do NOT commit or push. Nothing pushed to GitHub. User pushes manually.

### Accomplished
- Built the Home screen Figma-exact from the user's reference screenshot.
- Added a new **LOCKED "Home & Promotions"** contract section (`Offer`, `Testimonial`, `Referral` entities + `GET /offers`, `/testimonials`, `/referral`, all guest). `Testimonial` is distinct from the per-service `Review` (F10).
- New `src/features/home/` feature (types → Zod → api → hooks → components) + mocks.
- Reused `CategoryCard`, `ServiceCard`, catalog hooks, `DataState`, `Avatar`, `Card`, `Button`, `formatPrice`, toasts.
- Added optional `onOpen` to `ServiceCard` (backward-compatible, mirrors `ServiceListItem`).

### Key decisions (with user)
- Support FAB is **purple** per Figma (deviates from design.md line 14 green FAB — noted in checklist).
- Cart badge **hidden at count 0** (no cart until F13).
- Van "Arriving in 10 min" card **deferred to F15** (no tracking data/contract).
- Search bar → `/services` interim (F14 repoints to `/search`); cart/search/support taps use "coming soon" toast stubs.

### Next up
- F14 Search (small, reuses catalog components) or F7/F8/F9/F11/F13 (each needs its DRAFT contract section locked first).

## Technical State
### Files created
- `src/features/home/` — `types/home.ts`, `types/home.schema.ts`, `api/homeApi.ts`, `hooks/{keys,useFetchOffers,useFetchTestimonials,useFetchReferral}.ts`, `components/{SectionHeading,HomeHeader,HomeSearchBar,CategoryGridSection,PopularCombosSection,OfferCard,OffersSection,TestimonialCard,TestimonialsSection,ReferralCard,SupportFab}.tsx`, `index.ts`
- `src/mocks/data/{offers,testimonials,referral}.data.ts`
- `src/mocks/handlers/promotions.mock.ts` + `promotions.mock.test.ts`

### Files modified
- `instructions/contract.md` (Home & Promotions section + entities + change-log)
- `instructions/checklist.md` (F3 steps 32–42 → [x], contract status, progress 72/161 = 45%)
- `src/mocks/index.ts` (register promotions handler)
- `src/pages/HomePage.tsx` (composed all sections)
- `src/features/service-catalog/components/ServiceCard.tsx` (added `onOpen`)

### Tests
- `npm run typecheck` — clean
- `npm run build` — clean
- Vitest — **30/30 passing** (20 existing + 10 new promotions)
- Browser harness (scratchpad Playwright) — **32/32 passing**
- `npm run lint` — FAILS repo-wide (pre-existing; ESLint flat config never created; blocked on `typescript-eslint` package approval per Rule 10). Not caused by this work.

## Resume Instructions
1. Work in `BB-UI-Web/` (the real repo with the GitHub remote; parent "New folder (2)" is a separate local-only repo — do NOT work there).
2. Confirm branch: `git branch --show-current` → `feature/F03-home`.
3. Read `instructions/checklist.md` (F3 note) and this file.
4. If continuing verification: `npm run dev`, open http://localhost:5173 (mock mode).
5. Re-run tests: `npm run test -- --run`.
6. Do NOT commit/push unless the user explicitly says so.
