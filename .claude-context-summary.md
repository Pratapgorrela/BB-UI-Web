# Beauty Bus — Session Context Summary

## Session Summary
- **Date:** 2026-07-12/13 (prior sessions 2026-06-28)
- **Branch:** `feature/F06-auth` (stacked on `feature/F02-app-shell`, base main @ d0accbd)

### Completed Work
- **F0 — Scaffold** + **F1 — Design System**: complete (prior sessions, merged to main via PR #2)
- **F2 — App Shell & Navigation (Steps 25–31) COMPLETE this session**: `src/components/layout/` (AppShell, BottomNav, TopNav, Container, PageHeader, navItems). Pathless layout route wraps all pages; per-route `handle: { hideNav, fullBleed }` (auth pages bare, /dev/components full-bleed). 30 Playwright checks at 375/768/1024/1440px. **Pushed** to `origin/feature/F02-app-shell` — PR open, USER merges it themselves.
- **F6 — Auth (Steps 61–71) COMPLETE this session**: Auth contract LOCKED (2026-07-12). Real mock layer built — custom axios adapter `src/mocks/lib/mockEngine.ts` (no MSW, Rule 10), 300–600ms latency, contract-exact envelopes, localStorage sessions with refresh-token rotation. `useAuthStore` (zustand persist 'bb-auth'), apiClient Bearer injection + single-flight refresh-on-401 retry, auth API + 4 RQ hooks, Login/Register pages (RHF + Zod v4), ProtectedRoute (bookings/book/profile/notifications, returnTo), auth-aware TopNav, ToastProvider in App, minimal ProfilePage as /auth/me surface (F9 rebuilds). 20/20 Playwright checks, zero console errors. Typecheck + build green.
- Latent Tailwind v4 bug fixes: `z-sticky`/`z-toast` were no-ops (project tokens are `--z-*`; Tailwind resolves `--z-index-*` only) → use `z-(--z-sticky)` var shorthand; same for `max-w-(--width-max)`.

### In Progress
- Nothing half-done. F6 fully committed **LOCALLY** (74ea200, a563fc6, dacdc5d).

### Next (prioritized)
1. USER: test F6 on dev server (:5173), push `feature/F06-auth`, merge F2 PR then F6 PR
2. F4 Service Catalog — critical path (lock contract + Intel Report + approval before code)
3. F5 Details → F3 Home (F3 reuses F4's ServiceCard)

### Key Decisions
- F6 stacked on F02 branch (edits F2's router/TopNav); branch-from-main rule resumes after user merges.
- **USER INSTRUCTIONS: do NOT push F6 (user tests + pushes); do NOT merge PRs (user merges).**
- Login = email + password only (locked contract canonical over checklist "phone/email" wording).
- Mock pattern for all future features: `registerMock()` in `src/mocks/handlers/{resource}.mock.ts`.

### Known Issues
- `npm run lint` broken since F0: ESLint v10, no config, no typescript-eslint. Fix needs user approval (Rule 10): `typescript-eslint`, `eslint-plugin-react-hooks`, `globals` + eslint.config.js.
- Playwright MCP not connected → scratchpad playwright npm + `executablePath` at `%LOCALAPPDATA%\ms-playwright\chromium_headless_shell-1223`.

## Technical State
- Created: src/mocks/{lib/mockEngine.ts, data/users.data.ts, handlers/auth.mock.ts, index.ts}; src/features/auth/{types/auth.ts, types/auth.schema.ts, api/authApi.ts, hooks/*, index.ts}; src/store/useAuthStore.ts; src/types/api.ts; src/utils/apiError.ts; src/routes/ProtectedRoute.tsx; src/components/layout/* (F2).
- Modified: src/lib/apiClient.ts (adapter install replaces dead /mock-api hack), App.tsx, routes/index.tsx, TopNav, Toast, StickyBottomBar, Login/Register/Profile pages, placeholder pages (p-4→py-6), DevComponentsPage (+Layout showcase), instructions/contract.md, instructions/checklist.md (43/161 = 27%).
- No new npm packages. No unit tests yet; typecheck + build pass.
- Mock creds: priya@example.com/Priya@123 · rahul@example.com/Rahul@123 · error@test.com → 500 on register.

## Resume Instructions
1. Read this file, then instructions/checklist.md (F0, F1, F2, F6 done — 27%).
2. `git log --oneline -6` on feature/F06-auth; check if user pushed F6 / merged F2.
3. Follow instructions/MASTER_PROMPT.md init. Next feature F4: lock Service Catalog contract + Intel Report + wait for approval before code.
4. Auto-memory (project_beautybus_state.md) is current as of this session.
