# Beauty Bus — Session Context Summary

## Session Summary
- **Date:** 2026-07-13
- **Branch:** `feature/F04-service-catalog` (from up-to-date main @ d627f0b, F6 merged via PR #3)
- **Accomplished:** F4 Service Catalog COMPLETE — all steps 43–53. Progress 54/161 (34%). F0/F1/F2/F4/F6 done.
  - Step 43: Service Catalog contract AMENDED to Figma combo model then LOCKED (2026-07-13): `type` COMBO/SINGLE, `originalPrice`, `discountPercent`, `includedServiceIds`, `heroImageUrl`, `type` query param, INR
  - Steps 45–46: types + Zod schemas; 34 seed services / 6 categories (11 combos @20% off, Seniors has 0 combos = intentional empty-state); handlers GET /categories + GET /services (filter/search/sort/paginate, `search=FORCE_500` → 500)
  - Steps 47–48: catalogApi + catalogKeys + useFetchCategories/useFetchServices (staleTime 5min, keepPreviousData, `enabled` option)
  - Steps 49–53: ServiceCard, CategoryCard, CategoryFilterBar, CatalogControls (350ms debounced search + sort), CatalogPagination, ServiceCatalogPage (URL searchParams state), CategoryDetailPage at NEW route `/categories/:slug` (`/services/:id` reserved for F5)
- **In progress:** nothing dangling
- **Next (prioritized):** 1) USER pushes branch + opens/merges PR manually (explicit instruction: Claude must NOT push), 2) F5 Service Details (steps 54–60), 3) F3 Home (reuses ServiceCard/CategoryCard), 4) optional: fix lint (needs approval)
- **Key decisions:** contract amended while DRAFT then locked (Figma = source of truth, change-log row added); `/categories/:slug` route to avoid clashing with `/services/:id`; ServiceCard "+" shows "Cart is coming soon" toast until F13; serviceCount derived at serve time so data files can't drift
- **Bugs found/fixed:** mockEngine adapter IGNORED axios `config.params` — every GET query filter silently dropped in mock mode (latent since F6; auth used POST bodies). Fixed + added `paginated()` envelope helper
- **Blockers:** `npm run lint` fails repo-wide since F0 — ESLint 10 with NO flat config; fix needs `typescript-eslint` + `eslint-plugin-react-hooks` + `globals` (Rule 10: awaiting user approval)

## Technical State
- **Commits (local ONLY — user pushes manually):** 2ba4958 (Step 43), 2f60a05 (Steps 45-46), 5fbc104 (Steps 47-53 + tests), 900c953 (docs)
- **Created:** src/types/common.ts (Money); src/features/service-catalog/{types/catalog.ts, types/catalog.schema.ts, api/catalogApi.ts, hooks/{keys,useFetchCategories,useFetchServices}.ts, components/{ServiceCard,CategoryCard,CategoryFilterBar,CatalogControls,CatalogPagination}.tsx, index.ts}; src/mocks/data/{categories,services}.data.ts; src/mocks/handlers/catalog.mock.ts; src/mocks/handlers/catalog.mock.test.ts; src/pages/CategoryDetailPage.tsx; src/utils/format.ts (formatPrice ₹/formatDuration)
- **Modified:** src/mocks/lib/mockEngine.ts (params fix + paginated()), src/mocks/index.ts (register catalog.mock), src/pages/ServiceCatalogPage.tsx (full build), src/routes/index.tsx (/categories/:slug), instructions/contract.md, instructions/checklist.md
- **Deps:** none added (Rule 10 respected; playwright only in session scratchpad)
- **Tests:** 17/17 Vitest passing (`npm run test`, ~10s incl. simulated latency); typecheck + build clean; 37/37 browser checks (skeletons, 4 data states, filters, sort, pagination, toast, Men/Seniors/unknown-slug pages, 2/3/4 grid cols @768/1024/1440, zero overflow @375+, zero console errors happy path)

## Resume Instructions
1. Read instructions/checklist.md (F4 note has full detail) + this file
2. Check `git log --oneline -5` on feature/F04-service-catalog; ask user whether branch was pushed/merged before starting F5
3. Browser harness pattern: scratchpad `npm i playwright` (PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1), executablePath `%LOCALAPPDATA%\ms-playwright\chromium_headless_shell-1223\chrome-headless-shell-win64\chrome-headless-shell.exe`, target http://localhost:517x (check `npm run dev` output port)
4. PowerShell cwd RESETS between calls — always Set-Location into BB-UI-Web first (outer folder = different git repo)
5. F5 needs: GET /services/:id mock handler (contract section already LOCKED), ServiceDetailPage build, reuse ServiceCard/StickyBottomBar
