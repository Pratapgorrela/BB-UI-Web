# Beauty Bus — Session Context Summary

## Session Summary
- **Date:** 2026-07-13 (F5 Service Details session)
- **Branch:** `feature/F05-service-details` (off `main` after F4's PR #4 merged). **Committed LOCAL-ONLY — NOT pushed. User pushes manually; never push.**
- **Accomplished:**
  - **F5 Service Details COMPLETE** (steps 54–60). Commit `1ec3eca`.
    - Added `GET /services/:id` to `src/mocks/handlers/catalog.mock.ts` (found → single-resource envelope; unknown → 404 `RESOURCE_NOT_FOUND`; reserved id `FORCE_500` → 500). Contract Service Catalog already LOCKED — no contract change.
    - `fetchService(id)` in `catalogApi.ts` + new `useFetchService(id)` hook (reuses pre-existing `catalogKeys.service`). Barrel exports updated.
    - `ServiceDetailPage.tsx` rebuilt from placeholder → **immersive** (route handle `{ hideNav: true, fullBleed: true }`): full-bleed hero + back button (`navigate(-1)`), price row (strikethrough `originalPrice` + `DiscountBadge` + `formatDuration` + rating), description, combo-only **"What's included"** check-list, **"Recommended"** add-ons (reused `ServiceListItem`), reused `StickyBottomBar` ("Add to cart" → "Cart is coming soon" toast; real cart is F13).
    - Included/recommended derived client-side from ONE same-category `/services` query — **no invented contract field** (combo→`includedServiceIds`; single→other same-category singles ≤6).
    - **Nav wiring:** `ServiceListItem` gained optional `onOpen` (row tap → detail; `+` still toasts), used from `CategoryDetailPage`. Nothing linked to `/services/:id` before.
    - **KEY FIX (found via browser test):** a 404 is a React Query *error*, so it was routing to `DataState`'s error UI. Routed 404 → the **empty** branch (not-found + "Browse services" CTA) via `getApiError(err)?.code === 'RESOURCE_NOT_FOUND'`; only real failures show error/retry.
  - **Font + radius reduction** (user request) on BOTH immersive detail pages. Commit `6818f91`.
    - Titles h2→h3 (24→20px), price h3→h4 (20→18px), section headings h3→h4 (20→18px), description/list items body→body-sm (16→14px), sheet corner `rounded-t-3xl`→`rounded-t-xl` (24→16px via token scale). Applied to `ServiceDetailPage.tsx` AND `CategoryDetailPage.tsx` for consistency (user chose "also Category Detail").
- **In progress:** nothing dangling.
- **Next (prioritized):** 1) USER pushes `feature/F05-service-details` + opens/merges PR. 2) **F3 Home** (steps 32–42; reuses parked `ServiceCard` + `CategoryCard`; Service Catalog contract already LOCKED). 3) Optional lint fix (needs package approval, Rule 10). 4) Optional: real hero/thumbnail images (drop into same `imageUrl`/`heroImageUrl` fields, no code change).
- **Key decisions:** immersive detail (nav hidden) matching category detail; picsum imagery kept; add-ons derived client-side (no contract change); CTA "Add to cart"; 404→empty-state not error.
- **Blockers:** `npm run lint` still broken repo-wide (ESLint 10, no flat config; Rule 10 approval pending) — pre-existing, unrelated to F5.

## Technical State
- **Local commits (newest first):** `6818f91` (font/radius reduction, 2 files), `1ec3eca` (F5 service details, 10 files +343/−21). Both on `feature/F05-service-details`, NOT pushed.
- **Files created:** `src/features/service-catalog/hooks/useFetchService.ts`.
- **Files modified:** `src/mocks/handlers/catalog.mock.ts`, `src/mocks/handlers/catalog.mock.test.ts`, `src/features/service-catalog/api/catalogApi.ts`, `src/features/service-catalog/index.ts`, `src/features/service-catalog/components/ServiceListItem.tsx`, `src/pages/ServiceDetailPage.tsx`, `src/pages/CategoryDetailPage.tsx`, `src/routes/index.tsx`, `instructions/checklist.md`.
- **Patterns:** immersive detail page = `{ hideNav: true, fullBleed: true }` + full-bleed hero + `-mt-6 rounded-t-xl` content sheet. 404→empty via `getApiError().code`. Client-side derivation of related items from an existing endpoint (no new contract field).
- **Tests:** `npm run typecheck` clean; `vite build` clean; **20/20 Vitest** (17 existing + 3 new detail-endpoint: found/404/FORCE_500); **21/21 browser checks**. `npm run lint` still fails (pre-existing).
- **Progress:** 61/161 steps (38%). F0/F1/F2/F4/F5/F6 done.
- **Dependencies:** none added.

## Resume Instructions
1. Read this file + `instructions/checklist.md` (F5 completion note + progress summary).
2. `git log --oneline -4` on `feature/F05-service-details`; **ask the user whether the branch was pushed/PR'd before starting F3.**
3. Dev servers: user runs 5173; background Vite on 5174 (Playwright harness targets 5174).
4. Browser harness: current-session scratchpad has `verify-f5.js` (21 checks) + `shot.js` (screenshots; use `MSYS_NO_PATHCONV=1` when passing URL paths in Git Bash). Playwright-core borrowed from a prior scratchpad's `node_modules`; chrome exe `%LOCALAPPDATA%\ms-playwright\chromium_headless_shell-1223\...\chrome-headless-shell.exe`.
5. PowerShell/Bash gotcha: outer folder `New folder (2)` is a DIFFERENT git repo on `master`; the app repo is `BB-UI-Web/`. cwd resets between calls — cd into BB-UI-Web first.
6. **F3 Home** needs: HomePage compose (greeting header, search bar, category grid reusing `CategoryCard`, popular combos, offers, testimonials, referral, support FAB), 4 data states, responsive. Reuses parked `ServiceCard`. Follow MASTER_PROMPT: pull main, branch `feature/F03-home`, Intel Report + wait for approval, then build. **Never push.**
